version: "3.9"
services:
  api:
    volumes:
      - .:/app
    command: uvicorn graphait.main:app --host 0.0.0.0 --port 8000 --reload
  db:
    ports:
      - "5432:5432"
  redis:
    ports:
      - "6379:6379"
